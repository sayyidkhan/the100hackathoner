<footer class="footer">
  <div class="container footer-inner">
    <div class="footer-identity">
      <a class="footer-brand" href="/">The 100 Hackathoner</a>
      <span>A public record of ideas tested, teams formed, demos shipped, and lessons carried forward.</span>
    </div>
    <a class="footer-cta" href="/contact/">Invite me to speak, judge, mentor, or build <span aria-hidden="true">↗</span></a>
  </div>
</footer>

<style>
  .footer {
    border-top: 1px solid var(--line);
    padding: 24px 0;
    background: var(--black);
    color: white;
  }

  .footer-inner {
    display: grid;
    grid-template-columns: minmax(160px, 0.7fr) minmax(280px, 1.8fr) minmax(270px, 1.2fr);
    gap: 32px;
    align-items: center;
  }

  .footer-brand {
    display: inline-block;
    color: white;
    font-size: 0.92rem;
    font-weight: 900;
    letter-spacing: -0.02em;
  }

  .footer-identity {
    display: contents;
  }

  .footer-identity > span {
    color: rgba(255, 255, 255, 0.52);
    font-size: 0.8rem;
    line-height: 1.45;
    max-width: 43rem;
  }

  .footer-cta {
    color: var(--yellow);
    border-left: 1px solid rgba(255, 255, 255, 0.22);
    font-size: 0.9rem;
    font-weight: 800;
    letter-spacing: -0.01em;
    line-height: 1.25;
    padding-left: 32px;
    transition: color 160ms ease;
  }

  .footer-cta span {
    display: inline-block;
    margin-left: 3px;
    transition: transform 160ms ease;
  }

  .footer-cta:hover span {
    transform: translate(2px, -2px);
  }

  .footer-cta:hover {
    color: white;
  }

  @media (max-width: 760px) {
    .footer-inner {
      display: flex;
      align-items: flex-start;
      flex-direction: column;
      gap: 12px;
    }

    .footer-identity {
      display: contents;
    }

    .footer-cta {
      border-left: 0;
      border-top: 1px solid rgba(255, 255, 255, 0.22);
      padding: 12px 0 0;
    }
  }
</style>
